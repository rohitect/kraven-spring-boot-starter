server:
  port: 8081
  servlet:
    context-path: /

spring:
  application:
    name: kraven-ui-example
  main:
    allow-bean-definition-overriding: true

  # Kafka Configuration
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: kraven-example-group
      auto-offset-reset: earliest
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

# Application Configuration
app:
  api:
    base-url: http://localhost:8081

# Feign Client Configuration
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full
  httpclient:
    enabled: true
    max-connections: 200
    max-connections-per-route: 50

# Kraven UI Configuration
kraven:
  ui:
    path: /kraven
    development-mode: true
    logging:
      level: INFO
    theme:
      dark-primary-color: "#6c5ce7" # Purple
      dark-secondary-color: "#00b894" # Green
      dark-background-color: "#1a1b2e" # Dark blue
    business-flow:
      enabled: true
      base-packages:
        - io.github.rohitect.kraven.example
      show-detailed-method-info: true

# SpringDoc OpenAPI Configuration
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    enabled: false
  packages-to-scan: io.github.rohitect.kraven.example.controller
  paths-to-match: /**

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

# Logging Configuration
logging:
  level:
    io.github.rohitect.kraven: INFO
    org.springframework.web: INFO
    org.springframework.context.annotation: INFO
    org.springframework.beans.factory.support: INFO
    org.springframework.kafka: INFO
    org.apache.kafka: WARN
